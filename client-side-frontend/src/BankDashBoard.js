//import loginStore from "./LoginStore";
function BankDashboard(){
    //loginStore.subscribe(()=>{console.log(loginStore.getState())})
    //loginStore.dispath({type: 'default'})
    return <div>Bank Dashboard</div>
}

export default BankDashboard;